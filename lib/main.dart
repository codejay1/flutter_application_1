import 'package:flutter/material.dart';

void main(){

runApp(Myapp());
}

class Myapp extends StatelessWidget{

  Widget build(BuildContext context){

  return MaterialApp(home:Center(child: Text('My experience with flutter has been a good and funny one, because the environment is new, famillar with the framework but never used it, but i am happy i get to use it, thanks to the AIP. '),),);


  }
}